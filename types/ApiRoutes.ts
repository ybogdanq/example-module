//Because of ApiRoutes enum we can use route names inside Front-end services of course if monolith architecture used
export enum ApiRoutes {
  "DownloadCollectionCSV" = "/products/download-collection-csv/",
}
